<%= simple_form_for [current_user, @photo] do |f| %>

<div>
	<%= f.input :public, as: :boolean %>
</div>

<div>
	<%= f.input :caption, placeholder: 'type your caption here' %>
</div>

<div>
	<%= f.input :image, as: :file %>
</div>

<div>
	<%= f.button :submit %>
</div>

<% end %>